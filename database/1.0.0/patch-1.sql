DROP FUNCTION IF EXISTS public.add_reminder_date(character varying, character varying, character varying, character varying, character varying, character varying);

DROP FUNCTION IF EXISTS public.add_reminder_interval(character varying, character varying, character varying, character varying, character varying);

DROP FUNCTION IF EXISTS public.get_and_delete_expired_reminder();

DROP FUNCTION IF EXISTS public.get_language();

DROP FUNCTION IF EXISTS public.get_prefixes();


